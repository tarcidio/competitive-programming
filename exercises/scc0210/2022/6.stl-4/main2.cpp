#include <bits/stdc++.h>

using namespace std;
const int MAX = 1e9 + 1;
int snowflakes_id[MAX];//what is your name?

int main(){
    int num_cases, i, j, num_snowflakes, count, ans;
    cin >> num_cases;
    while(num_cases--){
        cin >> num_snowflakes;
        map<int,int> ma;//what is your name?
        count = ans = 0;
        for(int i = 0; i < num_snowflakes; i++)
            cin >> snowflakes_id[i];
        for(int j = 1; j <= num_snowflakes; j++){
            
        }
    }

    return 0;
}
