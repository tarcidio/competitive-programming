#include <bits/stdc++.h>

using namespace std;

int main(){
    
    //Linha para deixar o cin mais rápido
    ios:: sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, m, leitura;
    cin >> n >> m;
    
    for(int i = 0; i < n; i++){
        for(int j = 0; j < m; j++){
            cin >> leitura;
            if(leitura == 0){
                cout << i << ' ' << j;
                return 0;
            }
        }
    }
    
    return 0;
}
